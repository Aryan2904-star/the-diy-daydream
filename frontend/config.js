/* Local dev config — copy of config.example.js.
   This file is gitignored; edit it per environment. */
window.DDD_CONFIG = {
  // Same-origin: works on localhost AND through a tunnel/deploy automatically.
  API_URL: window.location.origin,
  SOCKET_URL: window.location.origin,
};
